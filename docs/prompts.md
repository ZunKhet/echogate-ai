# EchoGate Prompt Design

EchoGate uses structured prompts for:

1. Image analysis
2. First chapter generation
3. Next chapter generation
4. Story finalization

All AI responses should eventually be returned as JSON so the application can parse them reliably.