[project]
name = "echogate-ai"
version = "0.1.0"
description = "AI-powered interactive storytelling from images"
readme = "README.md"
requires-python = ">=3.11"

authors = [
    { name = "Zun Khet Wai" }
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]